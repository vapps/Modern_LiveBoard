<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="LiveBoard.View.CreatePage"
	DataContext="{Binding Source={StaticResource Locator}, Path=Main}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LiveBoard"
    xmlns:common="using:LiveBoard.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Page.Resources>

        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Style="{StaticResource LayoutRootStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top bar: Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Click="GoBack" IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}" Style="{StaticResource BackButtonStyle}"/>
            <TextBlock x:Name="pageTitle" Grid.Column="1" Text="Create" Style="{StaticResource PageHeaderTextStyle}"/>
        </Grid>
        <!-- 아이템의 드래그앤드롭 기능을 위해 ItemsSource 는 CodeBehind에서 설정한다. -->
        <ListView x:Name="ListViewPages" HorizontalAlignment="Left"
                  Height="608" Margin="40,26,0,-6" Grid.Row="1" 
                  VerticalAlignment="Top" Width="515"
                  CanReorderItems="True" CanDragItems="True" AllowDrop="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <StackPanel Orientation="Vertical" Background="CadetBlue">
                            <TextBlock Foreground="Red" Text="{Binding Path=Title}"/>
                            <TextBlock Foreground="Red" Text="{Binding Path=TemplateCode}"/>
                            <TextBlock Foreground="Red" Text="{Binding Path=Duration}"/>

                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Command="{Binding Path=AddPageCmd}" Content="Add Page" HorizontalAlignment="Left" 
                Margin="579,26,0,0" Grid.Row="1" VerticalAlignment="Top" Width="150" />
        <Button Command="{Binding Path=DeletePageCmd}" CommandParameter="{Binding ElementName=ListViewPages, Path=SelectedItem}" Content="Delete Page" HorizontalAlignment="Left" 
        	Margin="579,90,0,0" Grid.Row="1" VerticalAlignment="Top" Width="150"/>
		<Button x:Name="ButtonPreview" Command="{Binding Path=PreviewCmd}" Content="Preview" HorizontalAlignment="Left" 
        	Margin="579,150,0,0" Grid.Row="1" VerticalAlignment="Top" Width="150"/>
        <Button x:Name="ButtonPlay" Command="{Binding Path=PlayCmd}" Content="Play" HorizontalAlignment="Left" 
        	Margin="579,214,0,0" Grid.Row="1" VerticalAlignment="Top" Width="150"/>
		<Button x:Name="ButtonSave" Command="{Binding SaveCmd}" Content="Save" HorizontalAlignment="Left" 
        	Margin="579,293,0,0" Grid.Row="1" VerticalAlignment="Top" Width="150"/>
		<Button x:Name="ButtonLoad" Command="{Binding LoadCmd}" Content="Load" HorizontalAlignment="Left" 
			Margin="579,366,0,0" Grid.Row="1" VerticalAlignment="Top" Width="150"/>


		<Grid x:Name="GridPreview">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <Frame x:Name="FramePreview" 
                   HorizontalAlignment="Left" VerticalAlignment="Top" Height="351" Width="598" Margin="749,172,0,-493"/>
        </Grid>

		<VisualStateManager.VisualStateGroups>

            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape"/>
                <VisualState x:Name="Filled"/>

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PortraitBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!-- The back button and title have different styles when snapped -->
                <VisualState x:Name="Snapped">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pageTitle" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedPageHeaderTextStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>
